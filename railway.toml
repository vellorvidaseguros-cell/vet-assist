[build]
buildCommand = "npm install --omit=optional && cd frontend && npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/status"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "vet-assist"
